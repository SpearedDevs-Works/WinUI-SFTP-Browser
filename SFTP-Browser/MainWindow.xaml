<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="SFTP_Browser.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SFTP_Browser"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="SFTP-Browser">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar x:Name="AppTitleBar"
                  Grid.Row="0"
                  Title="SFTP Browser"
                  Subtitle="Preview"
                  VerticalContentAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  Margin="6,0"
                  Canvas.ZIndex="10">
        </TitleBar>

        <TabView x:Name="ShellTabView"
                 Grid.Row="1"
                 Loaded="ShellTabView_Loaded"
                 TabCloseRequested="ShellTabView_TabCloseRequestedAsync"
                 AddTabButtonClick="ShellTabView_AddTabButtonClick"
                 VerticalContentAlignment="Stretch"
                 VerticalAlignment="Stretch"
                 >
            <TabView.TabStripHeader>
                <Button Content="Settings" FontSize="11" Click="Settings_Click" Margin="6,0,0,-5">
                </Button>
            </TabView.TabStripHeader>
        </TabView>
    </Grid>
</Window>